const isDev = false

export const defaultPomodoroTime = isDev ? 60 : 1800
export const defaultCucumberTime = isDev ? 60 : 600
export const defaultTomatoCount = isDev ? 5 : 14
export const defaultAdditionalPomodoroTime = isDev ? 10 : 600
export const defaultAdditionalCucumberTime = isDev ? 5 : 300

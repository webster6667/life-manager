import { FC } from 'react'
import { Box } from '@mui/material'

export const PortalNodeWidget: FC = ({ engine, node }) => {
  return (
    <Box
      sx={{
        position: 'relative'
      }}
    ></Box>
  )
}

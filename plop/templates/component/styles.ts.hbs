import styled from "styled-components"
import { {{pascalCase mode}}{{pascalCase name}}Props } from "./types";

export const {{pascalCase name}} = styled.div<Omit<{{pascalCase mode}}{{pascalCase name}}Props, "">>`
    background: ${({theme}) => theme.palette.colors.primary};
`;

